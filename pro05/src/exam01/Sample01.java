package exam01;

public class Sample01 {

	public static void main(String[] args) { //자바 프로그램의 시작은 main**
		Person p = new Person();//펄슨이라는 클래스에 p라는 객체를 만드는것
		//클래스로 객체를 만드는 이러한 과정을 인스턴스화 한다고 한다.
		//p = 객체 = 인스턴스 -> 인스턴스p는 Heap메모리에 저장되어있다. 배열과 동일한 공간... 따라서 p에는 객체를 참조할 수 있는 참조값이 들어가 있다.
		// p.pNo = "";
		/* 
		 private String name; // 멤버 변수
		 private int age;
		 
		 public Member() {} //생성자
		 
		 public String getName() { //여기서부터  끝줄까지 멤버 메서드 
			 return name;
		 }
		 public String setName() { //이런 Sting setName 같은게 매개변수
			 this.name = name;
		 }
		 upblic int getAge() {
			 return age;
		 }
		 public void setAge(int age) {
			 this.age = age;
		 }
		 */
	}

}
